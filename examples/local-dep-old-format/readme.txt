
This workspace is how the 'local-dep' project would look like if it was created
by version v0.1.0-alpha9 (or earlier) of the poly tool.

If migrated, by executing 'poly migrate', it will look like the 'local-dep' project
and can be read by version v0.2.0-alpha10 and later.
