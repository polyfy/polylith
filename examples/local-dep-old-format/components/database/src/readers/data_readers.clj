(ns data-readers)

;; Clojure source files have to live under the top namespace.
;; The exception is the data-reader namespace ('data_readers.clj' and 'data_readers.cljc')
;; that is allowed to live in any namespace.
